#include <iostream>
using namespace std;

int main(int argc, char *argv[]) {
	int a = 0; //valor de un numero entero
	//un "=" para dar valores a una variable. 
	
	//Estructura condicionales
	if (a == 0){ //Si pasa algo vamos a ejecutar codigo
	//Pero 2 "==" es comparar
		cout <<"Se cumplio de que es 0";
	}
	else {
		cout <<"Tiene un valor diferente a 0";
	} //la estructura "else", siempre tiene que ir despues de un if
	
	return 0;
}

